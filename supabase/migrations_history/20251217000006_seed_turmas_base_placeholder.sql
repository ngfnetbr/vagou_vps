-- DDL-only migration (placeholder).
--
-- This project enforces "schema-only" migrations across municipalities.
-- Seed/reference data must be applied via per-city scripts (outside supabase/migrations).

